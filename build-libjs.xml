<?xml version="1.0"?>
<project name="build-libjs" default="zipall" basedir="."
 xmlns:if="ant:if"
    xmlns:unless="ant:unless">
  	<echo>Nothing to do</echo>

  	<!-- inputs directories -->
    <property name="resource.dir" value="resources" />  	
  	<property name="libjs.dir" value="libjs"/>
    <property name="swingjs.dir" value="swingjs"/>
	<!-- output directories -->
  	<property name="site.dir" value="site"/>
  	<property name="j2s.dir" value="${site.dir}/swingjs/j2s"/>
  	<property name="libjs.dir" value="libjs"/>

	<target name="zipall" depends="zipapache">
    </target>

  <target name="zipapache">
  </target>
</project>
